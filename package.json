{
  "name": "temporal-test",
  "version": "0.0.1",
  "description": "testing out temporal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "WolfScholle",
  "license": "ISC",
  "dependencies": {
    "proposal-temporal": "^0.9.0"
  }
}
